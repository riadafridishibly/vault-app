// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/riadafridishibly/vault-app/ent/masterpassword"
	"github.com/riadafridishibly/vault-app/ent/predicate"
)

// MasterPasswordQuery is the builder for querying MasterPassword entities.
type MasterPasswordQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.MasterPassword
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MasterPasswordQuery builder.
func (mpq *MasterPasswordQuery) Where(ps ...predicate.MasterPassword) *MasterPasswordQuery {
	mpq.predicates = append(mpq.predicates, ps...)
	return mpq
}

// Limit adds a limit step to the query.
func (mpq *MasterPasswordQuery) Limit(limit int) *MasterPasswordQuery {
	mpq.limit = &limit
	return mpq
}

// Offset adds an offset step to the query.
func (mpq *MasterPasswordQuery) Offset(offset int) *MasterPasswordQuery {
	mpq.offset = &offset
	return mpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mpq *MasterPasswordQuery) Unique(unique bool) *MasterPasswordQuery {
	mpq.unique = &unique
	return mpq
}

// Order adds an order step to the query.
func (mpq *MasterPasswordQuery) Order(o ...OrderFunc) *MasterPasswordQuery {
	mpq.order = append(mpq.order, o...)
	return mpq
}

// First returns the first MasterPassword entity from the query.
// Returns a *NotFoundError when no MasterPassword was found.
func (mpq *MasterPasswordQuery) First(ctx context.Context) (*MasterPassword, error) {
	nodes, err := mpq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{masterpassword.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mpq *MasterPasswordQuery) FirstX(ctx context.Context) *MasterPassword {
	node, err := mpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MasterPassword ID from the query.
// Returns a *NotFoundError when no MasterPassword ID was found.
func (mpq *MasterPasswordQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mpq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{masterpassword.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mpq *MasterPasswordQuery) FirstIDX(ctx context.Context) int {
	id, err := mpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MasterPassword entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MasterPassword entity is found.
// Returns a *NotFoundError when no MasterPassword entities are found.
func (mpq *MasterPasswordQuery) Only(ctx context.Context) (*MasterPassword, error) {
	nodes, err := mpq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{masterpassword.Label}
	default:
		return nil, &NotSingularError{masterpassword.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mpq *MasterPasswordQuery) OnlyX(ctx context.Context) *MasterPassword {
	node, err := mpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MasterPassword ID in the query.
// Returns a *NotSingularError when more than one MasterPassword ID is found.
// Returns a *NotFoundError when no entities are found.
func (mpq *MasterPasswordQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mpq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{masterpassword.Label}
	default:
		err = &NotSingularError{masterpassword.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mpq *MasterPasswordQuery) OnlyIDX(ctx context.Context) int {
	id, err := mpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MasterPasswords.
func (mpq *MasterPasswordQuery) All(ctx context.Context) ([]*MasterPassword, error) {
	if err := mpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return mpq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (mpq *MasterPasswordQuery) AllX(ctx context.Context) []*MasterPassword {
	nodes, err := mpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MasterPassword IDs.
func (mpq *MasterPasswordQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := mpq.Select(masterpassword.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mpq *MasterPasswordQuery) IDsX(ctx context.Context) []int {
	ids, err := mpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mpq *MasterPasswordQuery) Count(ctx context.Context) (int, error) {
	if err := mpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return mpq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (mpq *MasterPasswordQuery) CountX(ctx context.Context) int {
	count, err := mpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mpq *MasterPasswordQuery) Exist(ctx context.Context) (bool, error) {
	if err := mpq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return mpq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (mpq *MasterPasswordQuery) ExistX(ctx context.Context) bool {
	exist, err := mpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MasterPasswordQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mpq *MasterPasswordQuery) Clone() *MasterPasswordQuery {
	if mpq == nil {
		return nil
	}
	return &MasterPasswordQuery{
		config:     mpq.config,
		limit:      mpq.limit,
		offset:     mpq.offset,
		order:      append([]OrderFunc{}, mpq.order...),
		predicates: append([]predicate.MasterPassword{}, mpq.predicates...),
		// clone intermediate query.
		sql:    mpq.sql.Clone(),
		path:   mpq.path,
		unique: mpq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PasswordHash string `json:"password_hash,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MasterPassword.Query().
//		GroupBy(masterpassword.FieldPasswordHash).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mpq *MasterPasswordQuery) GroupBy(field string, fields ...string) *MasterPasswordGroupBy {
	grbuild := &MasterPasswordGroupBy{config: mpq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := mpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return mpq.sqlQuery(ctx), nil
	}
	grbuild.label = masterpassword.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PasswordHash string `json:"password_hash,omitempty"`
//	}
//
//	client.MasterPassword.Query().
//		Select(masterpassword.FieldPasswordHash).
//		Scan(ctx, &v)
func (mpq *MasterPasswordQuery) Select(fields ...string) *MasterPasswordSelect {
	mpq.fields = append(mpq.fields, fields...)
	selbuild := &MasterPasswordSelect{MasterPasswordQuery: mpq}
	selbuild.label = masterpassword.Label
	selbuild.flds, selbuild.scan = &mpq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a MasterPasswordSelect configured with the given aggregations.
func (mpq *MasterPasswordQuery) Aggregate(fns ...AggregateFunc) *MasterPasswordSelect {
	return mpq.Select().Aggregate(fns...)
}

func (mpq *MasterPasswordQuery) prepareQuery(ctx context.Context) error {
	for _, f := range mpq.fields {
		if !masterpassword.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mpq.path != nil {
		prev, err := mpq.path(ctx)
		if err != nil {
			return err
		}
		mpq.sql = prev
	}
	return nil
}

func (mpq *MasterPasswordQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MasterPassword, error) {
	var (
		nodes = []*MasterPassword{}
		_spec = mpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MasterPassword).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MasterPassword{config: mpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (mpq *MasterPasswordQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mpq.querySpec()
	_spec.Node.Columns = mpq.fields
	if len(mpq.fields) > 0 {
		_spec.Unique = mpq.unique != nil && *mpq.unique
	}
	return sqlgraph.CountNodes(ctx, mpq.driver, _spec)
}

func (mpq *MasterPasswordQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := mpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (mpq *MasterPasswordQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   masterpassword.Table,
			Columns: masterpassword.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: masterpassword.FieldID,
			},
		},
		From:   mpq.sql,
		Unique: true,
	}
	if unique := mpq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := mpq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, masterpassword.FieldID)
		for i := range fields {
			if fields[i] != masterpassword.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mpq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mpq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mpq *MasterPasswordQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mpq.driver.Dialect())
	t1 := builder.Table(masterpassword.Table)
	columns := mpq.fields
	if len(columns) == 0 {
		columns = masterpassword.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mpq.sql != nil {
		selector = mpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mpq.unique != nil && *mpq.unique {
		selector.Distinct()
	}
	for _, p := range mpq.predicates {
		p(selector)
	}
	for _, p := range mpq.order {
		p(selector)
	}
	if offset := mpq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mpq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MasterPasswordGroupBy is the group-by builder for MasterPassword entities.
type MasterPasswordGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mpgb *MasterPasswordGroupBy) Aggregate(fns ...AggregateFunc) *MasterPasswordGroupBy {
	mpgb.fns = append(mpgb.fns, fns...)
	return mpgb
}

// Scan applies the group-by query and scans the result into the given value.
func (mpgb *MasterPasswordGroupBy) Scan(ctx context.Context, v any) error {
	query, err := mpgb.path(ctx)
	if err != nil {
		return err
	}
	mpgb.sql = query
	return mpgb.sqlScan(ctx, v)
}

func (mpgb *MasterPasswordGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range mpgb.fields {
		if !masterpassword.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := mpgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mpgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (mpgb *MasterPasswordGroupBy) sqlQuery() *sql.Selector {
	selector := mpgb.sql.Select()
	aggregation := make([]string, 0, len(mpgb.fns))
	for _, fn := range mpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(mpgb.fields)+len(mpgb.fns))
		for _, f := range mpgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(mpgb.fields...)...)
}

// MasterPasswordSelect is the builder for selecting fields of MasterPassword entities.
type MasterPasswordSelect struct {
	*MasterPasswordQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mps *MasterPasswordSelect) Aggregate(fns ...AggregateFunc) *MasterPasswordSelect {
	mps.fns = append(mps.fns, fns...)
	return mps
}

// Scan applies the selector query and scans the result into the given value.
func (mps *MasterPasswordSelect) Scan(ctx context.Context, v any) error {
	if err := mps.prepareQuery(ctx); err != nil {
		return err
	}
	mps.sql = mps.MasterPasswordQuery.sqlQuery(ctx)
	return mps.sqlScan(ctx, v)
}

func (mps *MasterPasswordSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(mps.fns))
	for _, fn := range mps.fns {
		aggregation = append(aggregation, fn(mps.sql))
	}
	switch n := len(*mps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		mps.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		mps.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := mps.sql.Query()
	if err := mps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
